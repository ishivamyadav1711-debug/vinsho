---
import content from '../../vinsho-content.json';
import taxonomyData from '../../vinsho-taxonomy.json';
import Magnetic from './motion/Magnetic.astro';

const collections = taxonomyData.collections;
---

<div class="top-announcement-bar">
  <div class="top-bar-left">
    <a href={`mailto:${content.brand.email}`} class="top-email">
      <svg width="13" height="13" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8" style="margin-right: 0.35rem; vertical-align: middle;">
        <rect width="20" height="16" x="2" y="4" rx="2"/>
        <path d="m22 7-8.97 5.7a1.94 1.94 0 0 1-2.06 0L2 7"/>
      </svg>
      {content.brand.email}
    </a>
  </div>
  <div class="top-bar-center">
    <span>Express delivery &amp; free returns within 30 days</span>
  </div>
  <div class="top-bar-right">
    <a href={content.brand.facebook || "https://www.facebook.com/share/1Ed5e9L9sk/"} target="_blank" rel="noopener noreferrer" class="social-box" aria-label="Facebook">
      <svg width="12" height="12" viewBox="0 0 24 24" fill="currentColor"><path d="M18 2h-3a5 5 0 0 0-5 5v3H7v4h3v8h4v-8h3l1-4h-4V7a1 1 0 0 1 1-1h3z"/></svg>
    </a>
    <a href={content.brand.instagram || "https://www.instagram.com/vinsho_26"} target="_blank" rel="noopener noreferrer" class="social-box" aria-label="Instagram">
      <svg width="12" height="12" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.2" stroke-linecap="round" stroke-linejoin="round"><rect x="2" y="2" width="20" height="20" rx="5" ry="5"/><path d="M16 11.37A4 4 0 1 1 12.63 8 4 4 0 0 1 16 11.37z"/><line x1="17.5" y1="6.5" x2="17.51" y2="6.5"/></svg>
    </a>
  </div>
</div>

<header class="bar main-navbar">
  <div class="nav-brand">
    <a href="/" class="brand-badge-logo" aria-label="VINSHO Home">
      <img src="/vinsho-logo-transparent.png" alt="VINSHO" class="nav-logo-img" />
    </a>
  </div>
  
  <nav class="nav-links">
    <a href="/" class="nav-item" data-nav="home">Home</a>
    <a href="/about" class="nav-item" data-nav="about">About Us</a>
    <a href="/products" class="nav-item" data-nav="products">Products</a>
    <a href="/collections" class="nav-item" data-nav="collections">Collections</a>
    <a href="/blog" class="nav-item" data-nav="blog">Blog</a>
    <a href="/contact" class="nav-item" data-nav="contact">Contact Us</a>
  </nav>

  <div class="util">
    <a href="/products" class="util-link" id="header-search-btn" aria-label="Search catalogue">
      <svg class="ico" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
        <circle cx="11" cy="11" r="7"/>
        <path d="m20 20-3.5-3.5"/>
      </svg>
      <span class="util-text">Search</span>
    </a>
    <a href="/account" class="util-link" id="header-account-btn" aria-label="My Account">
      <svg class="ico" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
        <path d="M20 21v-2a4 4 0 0 0-4-4H8a4 4 0 0 0-4 4v2"/>
        <circle cx="12" cy="7" r="4"/>
      </svg>
      <span class="util-text" id="header-account-label">Account</span>
    </a>
    <a href="/cart" class="util-link" id="header-cart-btn" aria-label="View Cart">
      <svg class="ico" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
        <path d="M6 7h12l-1 13H7L6 7Z"/>
        <path d="M9 7a3 3 0 0 1 6 0"/>
      </svg>
      <span class="util-text">Cart</span>
      <span class="badge" id="cartN">0</span>
    </a>
    <Magnetic strength={3}>
      <a class="nav-cta-btn" href={`https://wa.me/${content.brand.whatsapp}?text=Hi%20VINSHO,%20I%20have%20an%20enquiry.`} target="_blank" rel="noopener noreferrer">
        Enquire
      </a>
    </Magnetic>
  </div>

  <!-- Mobile Hamburger Toggle Button -->
  <button class="mobile-hamburger-btn" id="mobile-menu-toggle" aria-label="Toggle navigation menu" aria-expanded="false" aria-controls="mobile-nav-drawer">
    <svg class="hamburger-icon" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
      <path class="line-top" d="M4 6h16" stroke-linecap="round"/>
      <path class="line-mid" d="M4 12h16" stroke-linecap="round"/>
      <path class="line-bot" d="M4 18h16" stroke-linecap="round"/>
    </svg>
  </button>
</header>

<!-- Mobile Navigation Backdrop -->
<div class="mobile-nav-backdrop" id="mobile-nav-backdrop" hidden aria-hidden="true"></div>

<!-- Mobile Full-Screen Slide-in Drawer -->
<aside class="mobile-nav-drawer" id="mobile-nav-drawer" hidden role="dialog" aria-modal="true" aria-label="Mobile Navigation Menu">
  <div class="drawer-header">
    <a href="/" class="brand-badge-logo" aria-label="VINSHO Home">
      <img src="/vinsho-logo-transparent.png" alt="VINSHO" class="nav-logo-img" style="height: 38px;" />
    </a>
    <button class="drawer-close-btn" id="mobile-drawer-close" aria-label="Close menu">
      <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
        <path d="M18 6L6 18M6 6l12 12" stroke-linecap="round" stroke-linejoin="round"/>
      </svg>
    </button>
  </div>

  <div class="drawer-body">
    <!-- Main Links -->
    <div class="drawer-section">
      <h3 class="drawer-heading">Navigation</h3>
      <nav class="drawer-nav-list">
        <a href="/" class="drawer-link" data-close-drawer>Home</a>
        <a href="/products" class="drawer-link" data-close-drawer>Products &amp; Catalogue</a>
        <a href="/account" class="drawer-link" data-close-drawer style="font-weight: 700; color: var(--maroon, #8A174B);">My Account &amp; Orders</a>
        <a href="/about" class="drawer-link" data-close-drawer>About Us</a>
        <a href="/collections" class="drawer-link" data-close-drawer>Collections Overview</a>
        <a href="/blog" class="drawer-link" data-close-drawer>In Real Homes (Blog)</a>
        <a href="/contact" class="drawer-link" data-close-drawer>Contact &amp; Showroom</a>
      </nav>
    </div>

    <div class="drawer-divider"></div>

    <!-- Top-Level Collections Navigation -->
    <div class="drawer-section">
      <h3 class="drawer-heading">Explore Collections</h3>
      <nav class="drawer-nav-list">
        {collections.map((col) => (
          <a href={`/collections/${col.key}`} class="drawer-link drawer-col-link" data-close-drawer>
            <span>{col.name}</span>
            <span class="col-flat-cnt">{col.count}</span>
          </a>
        ))}
      </nav>
    </div>

    <div class="drawer-divider"></div>

    <!-- Drawer Footer Actions -->
    <div class="drawer-footer-actions">
      <a 
        class="drawer-whatsapp-cta" 
        href={`https://wa.me/${content.brand.whatsapp}?text=Hi%20VINSHO,%20I%20have%20an%20enquiry.`} 
        target="_blank" 
        rel="noopener noreferrer"
        data-close-drawer
      >
        <svg width="18" height="18" viewBox="0 0 24 24" fill="currentColor">
          <path d="M12.012 2c-5.506 0-9.989 4.478-9.99 9.984a9.964 9.964 0 0 0 1.333 4.993L2 22l5.233-1.237a9.98 9.98 0 0 0 4.779 1.221h.004c5.505 0 9.987-4.478 9.989-9.984A9.96 9.96 0 0 0 12.012 2z"/>
        </svg>
        <span>Enquire via WhatsApp</span>
      </a>
      <p class="drawer-contact-note">
        📞 +91 9625515351 &middot; Greater Noida, NCR
      </p>
    </div>
  </div>
</aside>

<!-- Sticky WhatsApp / Call Bar (Mobile Only below 768px) -->
<div class="mobile-sticky-bar" id="mobile-sticky-bar">
  <a href="tel:+919625515351" class="sticky-bar-btn sticky-call">
    <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
      <path d="M22 16.92v3a2 2 0 0 1-2.18 2 19.79 19.79 0 0 1-8.63-3.07 19.5 19.5 0 0 1-6-6 19.79 19.79 0 0 1-3.07-8.67A2 2 0 0 1 4.11 2h3a2 2 0 0 1 2 1.72 12.84 12.84 0 0 0 .7 2.81 2 2 0 0 1-.45 2.11L8.09 9.91a16 16 0 0 0 6 6l1.27-1.27a2 2 0 0 1 2.11-.45 12.84 12.84 0 0 0 2.81.7A2 2 0 0 1 22 16.92z"/>
    </svg>
    <span>Call Us</span>
  </a>
  <a href={`https://wa.me/${content.brand.whatsapp}?text=Hi%20VINSHO,%20I%20am%20browsing%20the%20catalog.`} target="_blank" rel="noopener noreferrer" class="sticky-bar-btn sticky-wa">
    <svg width="16" height="16" viewBox="0 0 24 24" fill="currentColor">
      <path d="M12.012 2c-5.506 0-9.989 4.478-9.99 9.984a9.964 9.964 0 0 0 1.333 4.993L2 22l5.233-1.237a9.98 9.98 0 0 0 4.779 1.221h.004c5.505 0 9.987-4.478 9.989-9.984A9.96 9.96 0 0 0 12.012 2z"/>
    </svg>
    <span>WhatsApp Quote</span>
  </a>
</div>

<script>
  import { initCartStore } from '../scripts/cart-store';

  function setupNav() {
    initCartStore();

    const toggleBtn = document.getElementById('mobile-menu-toggle');
    const closeBtn = document.getElementById('mobile-drawer-close');
    const backdrop = document.getElementById('mobile-nav-backdrop');
    const drawer = document.getElementById('mobile-nav-drawer');

    const headerSearchBtn = document.getElementById('header-search-btn');
    if (headerSearchBtn) {
      headerSearchBtn.addEventListener('click', (e) => {
        if (window.location.pathname === '/products' || window.location.pathname === '/products/') {
          e.preventDefault();
          const qInput = document.getElementById('q') as HTMLInputElement | null;
          if (qInput) {
            qInput.focus();
            qInput.scrollIntoView({ behavior: 'smooth', block: 'center' });
          }
        }
      });
    }

    if (!toggleBtn || !drawer || !backdrop) return;

    let isTransitioning = false;

    function openDrawer() {
      if (isTransitioning) return;
      drawer.hidden = false;
      backdrop.hidden = false;
      drawer.setAttribute('aria-hidden', 'false');
      backdrop.setAttribute('aria-hidden', 'false');
      
      // Force layout flush for smooth transition
      void drawer.offsetWidth;
      
      drawer.classList.add('open');
      backdrop.classList.add('open');
      toggleBtn.setAttribute('aria-expanded', 'true');
      document.body.classList.add('lock');
    }

    function closeDrawer() {
      if (isTransitioning) return;
      isTransitioning = true;

      drawer.classList.remove('open');
      backdrop.classList.remove('open');
      document.body.classList.remove('lock');
      toggleBtn.setAttribute('aria-expanded', 'false');

      setTimeout(() => {
        if (!drawer.classList.contains('open')) {
          drawer.hidden = true;
          backdrop.hidden = true;
          drawer.setAttribute('aria-hidden', 'true');
          backdrop.setAttribute('aria-hidden', 'true');
        }
        isTransitioning = false;
      }, 350);
    }

    toggleBtn.addEventListener('click', (e) => {
      e.stopPropagation();
      if (drawer.classList.contains('open')) {
        closeDrawer();
      } else {
        openDrawer();
      }
    });

    closeBtn?.addEventListener('click', (e) => {
      e.stopPropagation();
      closeDrawer();
    });

    backdrop.addEventListener('click', (e) => {
      e.stopPropagation();
      closeDrawer();
    });

    // Close drawer when any internal link is clicked
    const drawerLinks = drawer.querySelectorAll('a, [data-close-drawer]');
    drawerLinks.forEach((link) => {
      link.addEventListener('click', () => {
        closeDrawer();
      });
    });

    document.addEventListener('keydown', (e) => {
      if (e.key === 'Escape' && drawer.classList.contains('open')) {
        closeDrawer();
      }
    });

    // Active nav link indicator on click & scroll
    const navItems = document.querySelectorAll<HTMLAnchorElement>('.nav-links .nav-item');
    navItems.forEach(item => {
      item.addEventListener('click', () => {
        navItems.forEach(link => link.classList.remove('active'));
        item.classList.add('active');
      });
    });

    const sections = document.querySelectorAll('section[id]');
    window.addEventListener('scroll', () => {
      let current = 'home';
      const scrollY = window.scrollY;
      
      sections.forEach(section => {
        const sectionHeight = (section as HTMLElement).offsetHeight;
        const sectionTop = (section as HTMLElement).offsetTop - 120;
        const sectionId = section.getAttribute('id');
        
        if (scrollY > sectionTop && scrollY <= sectionTop + sectionHeight) {
          if (sectionId === 'catalogue') current = 'catalogue';
          else if (sectionId === 'about') current = 'about';
          else if (sectionId === 'collections') current = 'collections';
          else if (sectionId === 'lookbook') current = 'lookbook';
          else if (sectionId === 'contact') current = 'contact';
        }
      });

      if (scrollY < 200) current = 'home';

      navItems.forEach(item => {
        if (item.dataset.nav === current) {
          item.classList.add('active');
        } else {
          item.classList.remove('active');
        }
      });
    }, { passive: true });
  }

  if (document.readyState === 'loading') {
    document.addEventListener('DOMContentLoaded', setupNav);
  } else {
    setupNav();
  }

  document.addEventListener('astro:page-load', setupNav);
</script>
